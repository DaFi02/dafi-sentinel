// Placeholder shell for the PR5 dashboard. The full routing tree,
// navigation, and pages land in the following work-unit commits; the
// scaffold commit only proves the Vite/React/Vitest setup wires up
// correctly.

export default function App() {
  return (
    <main className="app">
      <h1>DAFI Sentinel</h1>
      <p className="muted">Dashboard scaffold ready. Pages and auth gate land in the next commits.</p>
    </main>
  );
}
